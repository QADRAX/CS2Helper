/** `PrintWindow` flags (user32). */
export const PW_CLIENTONLY = 0x00000001;
export const PW_RENDERFULLCONTENT = 0x00000002;

/** `BitBlt` raster op (gdi32). */
export const SRCCOPY = 0x00cc0020;

/** `GetDIBits` usage. */
export const DIB_RGB_COLORS = 0;

/** `BITMAPINFOHEADER.biCompression` uncompressed RGB. */
export const BI_RGB = 0;
