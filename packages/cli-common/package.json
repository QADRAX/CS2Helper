{
  "name": "@cs2helper/cli-common",
  "version": "1.0.0",
  "description": "Shared Node CLI utilities for CS2Helper applications.",
  "main": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "pnpm run clean && vite build && tsc --emitDeclarationOnly",
    "clean": "rimraf dist",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "vite": "^8.0.10"
  }
}
