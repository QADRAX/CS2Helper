root = true

[*]
charset = utf-8

# Shell copied into Linux containers must use LF (CRLF breaks shebang: bash\r).
[apps/cs2-dedicated-server/scripts/*.sh]
end_of_line = lf
insert_final_newline = true
